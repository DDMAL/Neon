html
  block head
    title 404 - File not found
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.1/css/bulma.min.css')
  block body
    section.hero.is-info
      .hero-body
        .container
          h1.title
            | 404 - File Not Found
          h2.subtitle#fill-me
    script(type='text/javascript').
      let meifile = "#{meifile}";
      document.getElementById("fill-me").innerHTML = "The file <em>" + meifile + "</em> could not be found on this system!";
