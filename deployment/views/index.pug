doctype html
html
    head
        title Neon
        link(rel='icon', type='image/x-icon', href='favicon.ico')
        link(rel='shortcut icon', type='image/x-icon', href='favicon.ico')
        style
            include ./styles/landing.css
        script(defer=true, type='text/javascript', src='/Neon/landing.js')

    body
        .heading Neon 
               
        ul
            - for( var i = 0; i < files.length; i++ )
                li
                    = files[i]

        // ---- JUST TO UPLOAD FOR NOW-------
        section.container
            h3.subtitle
            | Single Upload:
            form(action='/upload_file', enctype='multipart/form-data', method='post')
                | select an MEI file to upload:
                input(type='file', name='resource')
                br
                | select a PNG file to upload:
                input(type='file', accept='image/png', name='resource')
                br
                input(type='submit', value='Upload')
        // ---------------------- *delete*
        
        br
        br
        br
        
        #select_documents_container
          #dropdown_label Uploaded Documents
          select#documents_dropdown(name='Uploaded Documents', multiple, size='15')
            optgroup#uploaded_folios(class='faded', label='Folios:')
            optgroup#uploaded_manuscripts(class='faded', label='Manuscript:')
          button#open_documents Open Documents
          button#delete_documents Delete Documents


        br
        br
        br

        #initial_upload_container 
        //- #files_unmatched
        //-     #mei_files_container 

        //-     #png_files_container 

        //-     #iiif_files_container
        #upload_container

        #message_container

        

